[package]
name = "uefi"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { workspace = true }
uefi = { version = "0.37.0", features = ["logger","panic_handler"]}
hvcore = { workspace = true }

